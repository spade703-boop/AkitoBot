# 剧情事件记忆重构计划

> 目标：让彰人在群友提到具体剧情经历时，能够回忆起“当时发生了什么、冬弥怎么反应、自己说了什么”，再以当前彰人的口吻自然复述。原始剧情是事实证据，模型只负责在证据范围内组织表达。
>
> 更新规则：每完成一个可验证的里程碑就勾选对应项，并记录日期、验证命令和已知限制。未经审核的候选内容不进入线上事件记忆库。

## 1. 数据职责

- [x] `data/content/akito_scripts.json` 继续作为彰人口吻、措辞和反应方式参考，不承担完整剧情事实。
- [x] `data/event_memory/story_import/drafts/` 保存可审核的完整剧情快照：日文原文、地区翻译、动作顺序、说话者和目标片段索引。
- [x] `data/content/akito_event_memories.json` 保存经过审核的“共同经历单元”摘要，供运行时只读召回。
- [x] 页面标题不作为事件事实、时间节点或剧情摘要来源；旧 `title` 字段仅作兼容性索引，运行时不得依赖它生成事实。
- [x] 外部 Wiki/资源站只作为导入源；bot 运行时使用本地快照，避免网络延迟、限流、版本漂移和服务不可用。

## 2. 剧情导入契约

- [x] 活动、组别、卡面、区域、自我介绍和特殊剧情统一解析为 `StoryRoute`。
- [x] 卡面 ID 优先从日服主数据定位，再读取日文 scenario 和可用的中文翻译；URL 中的 `zh-cn` 只表示页面语言。
- [x] 活动等非卡面剧情在目标地区缺失时回退到日服主数据和日服 scenario，翻译仍按页面地区尝试读取。
- [x] 草稿保留完整有序 `actions`，每条记录包含说话者、日文、中文和 `kind`（对白/内心/旁白）。
- [x] `target_segments` 只筛选彰人/冬弥相关片段，避免把心羽、杏等其他角色的整段台词作为目标记忆。
- [x] 对长剧情按“一个场景中的一次互动”拆成多个候选单元，而不是把整章压成一条摘要。
- [x] 每个候选单元至少包含：情景与冬弥行为、彰人关键回应、双方可追溯的动作索引；缺少一方时标记为待审核，不自动发布。

## 3. EventMemory 兼容格式

- [x] 保留现有事件级字段（`event_id`、`source`、`summary`、`topics`、`confidence`、`evidence` 等），避免影响旧数据读取。
- [x] 一个故事可以有多个 `evidence` 单元；每个单元的 `context` 保存共同情景、冬弥行为和必要背景，`dialogue` 只保存彰人关键回应。
- [x] 事件记忆中的文本使用精简中文；完整双语原文和动作索引留在对应草稿及缓存资产中，通过 `source.draft_id` 和 `record_indices` 追溯。
- [x] 新记录不使用页面标题作为 `summary` 的兜底事实；没有人工/审核摘要时保持待审核，不发布。
- [x] 发布前执行 schema、证据引用、目标说话者和去重校验；同一 scenario 的内容变更必须生成修订快照并显式确认。

## 4. 迁移与去重

- [x] 先为 `event/71/8` 生成多单元候选，与现有单条事件做 diff，人工确认后再替换。
- [ ] 保留 `akito_scripts.json` 不变；旧事件库记录暂不批量重写，按来源和置信度分批迁移。
- [ ] 以规范化双语动作序列计算内容摘要，以目标片段计算证据摘要；跨地区重复内容合并来源，不重复注入。
- [ ] 记录每次发布的 `source_hash`、`content_digest`、`evidence_digest` 和修订快照，支持回滚与复核。

## 5. 运行时召回

- [x] 事件记忆以只读方式检索，低置信度或模糊问题不注入 Prompt。
- [x] 同一事件命中时展示 2～3 个相关互动单元，而不是固定只取首条证据。
- [ ] Prompt 明确区分：原作事实、彰人表达参考、群友当前说法和不确定内容；模型可改写，不要求逐字复述。
- [ ] 群友确认某段经历后，只把 `event_id`/事件主题写入短期会话状态，不自动生成新的长期事实。
- [ ] 用户提出资料未覆盖的细节时，保留“不确定/待核对”状态，不用模型猜测覆盖原作事实。

## 6. 评测与发布

- [ ] 为每个共同经历单元生成正例、相邻场景例和事实不符负例；评测基线使用相近场景原文作为“态度/思路”参照，不做逐字匹配。
- [x] 检查主动对话、自动回复、群印象三种 surface 是否共享同一事件召回结果，同时保留各自触发和输出约束。
- [ ] 先离线评测和本地草稿审核，再用现有单群 `combined` 灰度观察；稀疏线上 trace 不阻塞开发，只暂缓扩大放量。
- [ ] 每次批量迁移后重建索引、运行回归测试，并在本文件记录覆盖数量、召回指标和已知缺口。

## 7. 里程碑记录

### M2-A：导入与首个多单元事件

- [x] `event/71/8` 完成双方共同经历单元候选。
- [x] 人工确认后发布到本地事件库，旧记录可通过修订快照恢复。
- [x] 验证命令：`pytest -q tests/core/test_story_import.py tests/core/test_event_memory.py`（42 passed）。

### M2-B：运行时多单元召回

- [x] 事件检索结果暴露单元级证据，Prompt 至少可见情景、冬弥行为和彰人回应。
- [x] 验证主动对话、自动回复、群印象共享召回入口；trace 新增 `event_evidence_units` 记录本轮实际注入的事件单元索引。

### M2-C：批量覆盖与持续维护

- [ ] 依据来源×时间线×参与者×事件类型覆盖矩阵补录剧情。
- [ ] 对未命中问题、用户纠正和内容修订建立离线补录队列。
- [ ] 通过离线评测后再扩大灰度范围。

### M2-D：事件检索统一与降噪

- [x] 为事件补充 `source_kind` 与 `review_status`，区分人工审核剧情和旧脚本生成内容。
- [x] 先对词法候选逐条执行绝对分、相对分差和来源优先级过滤，不再固定补满 Top-K。
- [x] 将 EventMemory 注册为统一检索语料，支持词法与向量 Top-20 合并后使用原始问题 rerank。
- [x] rerank 或索引不可用时回退安全词法结果，不回退未经逐条过滤的 cosine Top-K。
- [x] Prompt 明确区分人工审核剧情与旧脚本自动整理片段，未审核内容不再包装成已确认事实。
- [x] EventMemory 向量索引必须完整覆盖全部事件，任一 embedding 失败都拒绝替换正式索引；运行时也拒绝不完整或形状异常的索引。
- [x] 词法与 Hybrid 分别使用适合其分数尺度的人工剧情优先差值，避免误遮蔽更相关的旧脚本候选。
- [x] trace 记录候选来源、各阶段分数、保留状态和丢弃原因，不记录用户或剧情原文。
- [x] 检索构建与评测工具归入 `tools/event_memory/retrieval/`，测试放入对应镜像目录。
- [x] 生产环境部署时构建 `event_memory_embeddings.npz`；索引保持 Git 忽略并接受指纹校验。
- [x] 定向误召回、三种 surface、完整回归、Ruff 与 `git diff --check` 全部通过后记录结果。

## 8. 当前实施记录

### 2026-08-27

- 完成 `event/71/8` 的 4 个双方共同经历单元发布；事件卡不再把页面小节标题当作事实字段，记录约 2.3 KB。
- 运行时事件证据最多展示命中事件的 3 个单元，并按当前问题对单元排序；trace 新增 `event_evidence_units`，不记录原文内容。
- 主动对话、自动回复、群印象均通过同一事件召回入口，并写入事件单元观测字段。
- 已验证：`ruff check`、Python 编译检查、`git diff --check` 通过；`pytest -q` 共 767 项通过。
- 已知边界：事件库的旧记录仍保持原格式，后续按来源和人工审核结果分批迁移；完整双语原文仍以草稿快照为准。
- 完成本轮文件归类：对话基线、灰度工具、事件记忆和剧情导入分别集中到 `tools/`、`tests/`、`docs/`、`data/` 下的同名功能目录；移除剧情工具重复入口和无用构建日志。

### 2026-08-29

- 完成 M2-D 事件检索统一：826 条事件全部标注来源与审核状态；人工剧情和旧脚本使用同一词法评分规则，人工剧情只有在显著更相关时才遮蔽旧候选。
- EventMemory 已注册为第三个语义检索语料；`AKITO_EVENT_MEMORY_RETRIEVAL=hybrid` 时合并词法/向量 Top-20 后用原问题 rerank，索引或 rerank 不可用时回退安全词法。
- 离线 44 条评测：Recall@1 0.8519、Recall@3 1.0、MRR 0.9259、负例误认率 0、模糊问法拒绝率 1.0；“发烧”只召回对应人工剧情，不补入蘑菇或普通练习事件。
- 已验证：826 条库存迁移仅新增来源元数据、未重写剧情文本；`python tools/event_memory/retrieval/build_index.py --check`、完整 `pytest -q` 781 项、`ruff check .` 与 `git diff --check` 全部通过。
