[project]
name = "akito_bot"
version = "0.4.5"
description = "akito_bot"
readme = "README.md"
requires-python = ">=3.9, <4.0"
dependencies = [
    "nonebot2[fastapi]>=2.4.4",
    "nonebot-adapter-onebot>=2.4.6",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "Pillow>=10.0.0",
    "aiohttp>=3.9.0",
    "aiosqlite>=0.20.0",
    "numpy>=1.21.0",
    "jinja2>=3.1.0",
    "nonebot-plugin-htmlrender>=0.3.0",
    "nonebot-plugin-alconna>=0.50.0",
    "nonebot-plugin-apscheduler>=0.4.0",
    "nonebot-plugin-uninfo>=0.7.0"
]

[tool.nonebot]
plugin_dirs = []
builtin_plugins = []

[tool.nonebot.adapters]
nonebot-adapter-onebot = [
    { name = "OneBot V11", module_name = "nonebot.adapters.onebot.v11" }
]
"@local" = []

[tool.nonebot.plugins]
"@local" = []

# ── Dev Dependencies ──────────────────────────────────────────────────────

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.5.0",
    "mypy>=1.10.0,<2.0",
]

# ── Ruff Linter ───────────────────────────────────────────────────────────

[tool.ruff]
target-version = "py39"
line-length = 120

[tool.ruff.lint]
select = [
    "E",     # pycodestyle errors
    "W",     # pycodestyle warnings
    "F",     # Pyflakes (unused imports, undefined names)
    "I",     # isort (import ordering)
    "N",     # pep8-naming conventions
    "UP",    # pyupgrade (modern Python syntax)
    "B",     # flake8-bugbear (common bugs)
    "SIM",   # flake8-simplify (code simplification)
    "T20",   # flake8-print (no print statements)
]

ignore = [
    "E501",  # line too long (handled by formatter)
    "E701",  # multiple statements on one line (colon) — 既有紧凑写法，可接受
    "E702",  # multiple statements on one line (semicolon) — 既有紧凑写法，可接受
    "UP007", # Use X | Y for type annotations (keep Optional for py39 compat)
    "N806",  # 函数内常量性局部变量允许 UPPER_SNAKE_CASE（见 PROJECT_SPEC §3）
    "N812",  # lowercase imported as non-lowercase (NoneBot2 convention)
    "B008",  # do not perform function calls in argument defaults (false positives with nonebot)
    "SIM105",# 允许尽力而为的 try/except/pass 静默抑制（见 PROJECT_SPEC §8.1）
]

[tool.ruff.lint.isort]
known-first-party = ["nonebot_plugin_akito"]
force-sort-within-sections = true

[tool.ruff.lint.per-file-ignores]
"tests/core/test_time_awareness.py" = ["E402"]  # 导入依赖 conftest.py 的 mock 初始化
"tests/*.py" = ["SIM117"]                       # 嵌套 with mock.patch 是可接受的测试写法
"tests/**/*.py" = ["SIM117"]
"tests/features/random_paro/test_helpers.py" = ["N802", "N803"]  # 保留中文测试名与夹具名，避免无意义重命名
"**/__init__.py" = ["E402", "F401", "I001"] # NoneBot 插件需在元数据后做副作用导入、按依赖顺序再导出，不参与自动排序
"tools/*.py" = ["T201"]                     # CLI 工具允许 print

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "lf"

# ── Mypy Type Checker ─────────────────────────────────────────────────────

[tool.mypy]
python_version = "3.9"
warn_return_any = false
warn_unused_configs = true
ignore_missing_imports = true
follow_imports = "silent"
strict = false
check_untyped_defs = false
disallow_untyped_defs = false
files = [
    "nonebot_plugin_akito/core/types.py",
    "nonebot_plugin_akito/core/game_store.py",
    "nonebot_plugin_akito/core/memory.py",
    "nonebot_plugin_akito/features/gift/types.py",
    "nonebot_plugin_akito/features/gift/logic.py",
    "nonebot_plugin_akito/features/rpg/types.py",
    "nonebot_plugin_akito/features/rpg/player.py",
    "nonebot_plugin_akito/features/rpg/inventory.py",
    "nonebot_plugin_akito/features/rpg/fortune.py",
    "nonebot_plugin_akito/features/rpg/rewards.py",
    "nonebot_plugin_akito/features/rpg/state.py",
    "nonebot_plugin_akito/features/rpg/utils.py",
    "nonebot_plugin_akito/features/rpg/combat.py",
    "nonebot_plugin_akito/features/rpg/boss.py",
    "nonebot_plugin_akito/features/rpg/analytics.py",
    "nonebot_plugin_akito/features/rpg/team.py",
    "nonebot_plugin_akito/features/scheduled/__init__.py",
]

[[tool.mypy.overrides]]
module = [
    "nonebot.*",
    "nonebot_plugin_alconna.*",
    "nonebot_plugin_apscheduler.*",
    "nonebot_plugin_htmlrender.*",
    "nonebot_plugin_uninfo.*",
    "openai.*",
    "PIL.*",
    "aiohttp.*",
]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = [
    "nonebot_plugin_akito.core.game_store",
    "nonebot_plugin_akito.core.memory",
    "nonebot_plugin_akito.features.gift.logic",
    "nonebot_plugin_akito.features.rpg.player",
    "nonebot_plugin_akito.features.rpg.inventory",
    "nonebot_plugin_akito.features.rpg.fortune",
    "nonebot_plugin_akito.features.rpg.rewards",
    "nonebot_plugin_akito.features.rpg.state",
    "nonebot_plugin_akito.features.rpg.utils",
    "nonebot_plugin_akito.features.rpg.combat",
    "nonebot_plugin_akito.features.rpg.boss",
    "nonebot_plugin_akito.features.rpg.analytics",
    "nonebot_plugin_akito.features.rpg.team",
    "nonebot_plugin_akito.features.scheduled",
]
check_untyped_defs = true

# ── Pytest ────────────────────────────────────────────────────────────────

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
pythonpath = ["."]
norecursedirs = ["nonebot_plugin_akito", "data", ".claude"]
